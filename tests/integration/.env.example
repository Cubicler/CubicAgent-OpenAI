# Integration Test Environment Configuration
# Copy this file to .env.local and fill in your actual API key for integration testing

# Your OpenAI API key (required for integration tests) - get from https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# OpenAI model for testing (using faster/cheaper model for tests)
OPENAI_MODEL=gpt-3.5-turbo

# Low temperature for more deterministic test results
OPENAI_TEMPERATURE=0.1

# Reduced token limit for faster test execution
OPENAI_SESSION_MAX_TOKENS=150

# Extended timeout for integration tests to account for API latency
AGENT_TIMEOUT=15000

# Fewer retries for faster test failure detection
AGENT_MAX_RETRIES=2

# Reduced iteration limit for faster test execution
AGENT_SESSION_MAX_ITERATION=5

# Error-only logging to reduce test output noise
LOG_LEVEL=error
